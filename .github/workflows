name: GitHub Actions Demo
run-name: For-practice
on: [push]

env:
  IMAGE_NAME: adityavishwakarma31/django-todo-app
jobs:
  build:
    runs-on: self-hosted
    steps:
      - name: pull repo
        run: git clone https://github.com/Adityavishwakarma31/ci-cd-project.git
      - name: Build images
        run: 
      - name: list of images
        run: docker images
