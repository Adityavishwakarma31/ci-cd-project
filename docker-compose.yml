version: "3.9"

services:
  web:
    build: .
    container_name: todo-app
    ports:
      - "8000:8000"
    volumes:
      - sqlite_data:/app/data
    environment:
      - DEBUG=False
    restart: unless-stopped

volumes:
  sqlite_data:
